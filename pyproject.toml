[project]
name = "numcompute-stream"
version = "0.1.0"
description = "Streaming decision tree + ensemble framework (NumPy + matplotlib only)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"